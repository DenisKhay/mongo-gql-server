{
    "version": "2.0.0",
    "command": "npm",
    "isShellCommand": true,
    "showOutput": "always",
    "suppressTaskName": true,
    "options": {
        "cwd": "${workspaceFolder}/mongo-gql"
    },
    "tasks": [
        {
            "label": "build:debug",
            "args": [ "run", "build:debug" ],
            "isBuildCommand": true
        }
    ]
}